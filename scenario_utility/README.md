# Scenario Utility

The scenario_exection repository from IntelLabs have been removed, so therefore the code of that repository needed to be copied over into this repo. It is added in this separate directory so it does not clutter the actual codespace since no changes is needed to be made in this.
